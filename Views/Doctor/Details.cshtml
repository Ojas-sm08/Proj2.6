@model HospitalManagementSystem.Models.Doctor
@* Ensure no extra characters or spaces after the model declaration above. *@

@{
    ViewData["Title"] = $"Doctor Details: {Model.Name}";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="mb-4 text-center">👨‍⚕️ Doctor Details</h2>

<div class="card shadow-sm p-4">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Id) @* Changed to m => m.Id for consistency and robustness *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Id) @* Changed to m => m.Id *@
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Name) @* Changed to m => m.Name *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Name) @* Changed to m => m.Name *@
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Specialization) @* Changed to m => m.Specialization *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Specialization) @* Changed to m => m.Specialization *@
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Description) @* Changed to m => m.Description *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Description) @* Changed to m => m.Description *@
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Contact) @* Changed to m => m.Contact *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Contact) @* Changed to m => m.Contact *@
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(m => m.Location) @* Changed to m => m.Location *@
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(m => m.Location) @* Changed to m => m.Location *@
            </dd>
        </dl>
    </div>
    <div class="card-footer text-center">
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning me-2"><i class="fas fa-edit me-1"></i> Edit</a>
        <a asp-action="Manage" class="btn btn-secondary"><i class="fas fa-arrow-left me-1"></i> Back to List</a>
    </div>
</div>

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js" crossorigin="anonymous"></script>
}
